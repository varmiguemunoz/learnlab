# --- IMAP (entrada) ---
IMAP_HOST=imap.example.com
IMAP_PORT=993
IMAP_USER=usuario@example.com
IMAP_PASS=tu_password_imap
IMAP_MAILBOX=INBOX

# --- SMTP (salida) ---
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=usuario@example.com
SMTP_PASS=tu_password_smtp
SMTP_FROM=alertas@example.com

# Múltiples destinatarios separados por coma
ALERT_RECIPIENTS=destino1@example.com,destino2@example.com

# Múltiples keywords separadas por coma
KEYWORDS=DAVIVIENDA

# Intervalo de polling en segundos
POLL_INTERVAL=60

# Notificaciones de escritorio en macOS si falla el envío SMTP
DESKTOP_NOTIFICATIONS=true

# Opcionales
# STATE_FILE=/ruta/absoluta/processed_uids.txt
# LOG_FILE=/ruta/absoluta/monitor.log
# LOCK_DIR=/ruta/absoluta/.monitor.lock
